# Video quiz form

- Install dependencies and build the component:

```sh
npm install
npm run build 
```

- Navigate to the example folder and try it:

```sh
cd video-quiz
npm install
npm run build
npm start
```
